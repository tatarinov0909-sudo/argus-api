/* eslint-disable camelcase */

exports.shorthands = undefined;

// Откуда взялся остаток в ячейке.
//
// NULL — работник положил сюда товар своими руками через приёмку. Это
// единственное, что Аргус знает достоверно.
//
// '1c' — выведено из учёта владельца: 1С говорит, что товара всего N и что
// лежит он в одной-единственной ячейке, значит все N там. Вывод сильный, но
// это всё-таки вывод, а не проверенный факт: полку никто не видел.
//
// Колонка нужна ровно затем, чтобы эти две вещи нельзя было перепутать.
// Утром этого дня я стёр 879 остатков, про которые уже никто не мог сказать,
// откуда они взялись, — и весь склад полгода жил на выдумке. Одно поле
// дешевле, чем повторение той истории.
exports.up = (pgm) => {
  pgm.sql(`ALTER TABLE cell_stock ADD COLUMN IF NOT EXISTS source TEXT;`);
};

exports.down = (pgm) => {
  pgm.sql('ALTER TABLE cell_stock DROP COLUMN IF EXISTS source;');
};
