/* eslint-disable camelcase */

exports.shorthands = undefined;

// Штрихкод и зарезервированное количество у товара.
//
// ШТРИХКОД — единственный код, одинаковый во всех системах, потому что
// напечатан на коробке. На нём держатся две вещи, которых сейчас нет:
// сканирование при приёмке и сборке, и выгрузка остатков в 1С продавца (его
// коды с нашими не совпадают, а штрихкод совпадает всегда).
//
// РЕЗЕРВ — в 1С проведённый заказ покупателя жёстко закрепляет товар. Аргус
// про это не знает и видит только физический остаток, поэтому может
// предложить работнику взять то, что уже обещано другому заказу. Проявится в
// первый же день, когда на один дефицитный артикул придут два заказа.
//
// Оба поля приходят из 1С и живут рядом с остальной карточкой. Резерв —
// именно снимок на момент обмена, а не наша величина: считать его самим
// значило бы спорить с 1С о том, что она и так знает лучше.
exports.up = (pgm) => {
  pgm.sql(`
    ALTER TABLE products
      ADD COLUMN barcode TEXT,
      ADD COLUMN reserved_qty NUMERIC,
      ADD COLUMN reserved_at TIMESTAMPTZ;

    -- Поиск товара по штрихкоду — то, что делает сканер на каждую коробку.
    -- Уникальность НЕ ставим: в живых базах один штрихкод у нескольких
    -- карточек встречается сплошь, и падать на обмене из-за этого нельзя.
    CREATE INDEX idx_products_barcode ON products(warehouse_id, barcode)
      WHERE barcode IS NOT NULL;
  `);
};

exports.down = (pgm) => {
  pgm.sql(`
    DROP INDEX IF EXISTS idx_products_barcode;
    ALTER TABLE products
      DROP COLUMN IF EXISTS barcode,
      DROP COLUMN IF EXISTS reserved_qty,
      DROP COLUMN IF EXISTS reserved_at;
  `);
};
