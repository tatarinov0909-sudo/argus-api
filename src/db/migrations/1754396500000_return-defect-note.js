/* eslint-disable camelcase */

exports.shorthands = undefined;

// «Брак фиксируется с фото и описанием дефекта — не просто "испорчено", а
// видно, что именно» — требование операционной модели. Количество у брака
// было, причины не было: продавец получал «2 шт брак» и не мог решить, вернуть
// товар себе на производство или утилизировать, не приехав смотреть.
//
// Текст, а не справочник причин: перечислить заранее все способы испортить
// товар нельзя, а работник у полки описывает словами быстрее, чем ищет пункт
// в списке. Появится статистика — из неё и вырастет справочник.
//
// Фото этого же дефекта — отдельная история: файлы в Аргусе не хранятся
// нигде, и решение, где им жить, ещё не принято. Здесь только текст.
exports.up = (pgm) => {
  pgm.sql(`
    ALTER TABLE return_records ADD COLUMN defect_note TEXT;
  `);
};

exports.down = (pgm) => {
  pgm.sql(`
    ALTER TABLE return_records DROP COLUMN IF EXISTS defect_note;
  `);
};
